import styled from '@emotion/styled';
import { Box } from '@mui/material';

const StyledWrapperCointainer = styled(Box)`
  width: 100%;
  display: inline-block;
  gap: 24px;
  opacity: 0px;
`;

export { StyledWrapperCointainer };
