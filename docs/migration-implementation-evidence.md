# Migration implementation evidence — work in progress

This is an evidence ledger, not a release acceptance statement. The central populated V1→V2→V3 acceptance test has **not yet passed**. No mainnet deployment, live authority change, or real user funds are involved.

## Source and fixtures

- Starting main commit: `fde2635138f60f6d14954b2f91e4a001b74c12a2`.
- Feature: `feat/462-state-preserving-redeployment`, separate worktree; unrelated `feat/host-state-full-key-trie` changes remain untouched.
- The initial migration-capable devnet blueprint is pinned with SHA-256 `fb99d2dea3fb521c8e89797355b77567c99942737aa7493707a4f970a4577bf1`. It has not been overwritten to manufacture compatibility evidence.
- The optimized baseline candidate has SHA-256 `100d902fc6f3c401bb0004133c2c78a8de54e9a84b4c82b07b56bfc30bec9807`. Its changed immutable policies require a **distinct fresh baseline**. This is not a migration of the initial deployment.

## Actual cross-chain execution so far

The isolated five-pool Cardano network and pinned ibc-go v8 counterparty have accepted two open channels using the real Cardano light-client verification path. On the initial baseline, Cardano transaction `83f0519740d46d7f5251905bc13903b563c91d04eab362215ba470e1370657ed` escrowed 10,000,000 lovelace. Cosmos accepted its receive at height 969 and issued 10,000,000 units of `ibc/31DBB53F8D9E82C982C6F2549667B3FE394C4C63F73F7576CBF9F72A5464EC43`. The acknowledgement remains pending. These escrow and voucher claims have not been migrated or replaced.

Two foreign-origin sends (1,000,000 and 200,000 stake) remain pending on that original route. Their first Cardano voucher receive exposed a real pre-existing integration failure: the Gateway attempted a standalone trace-registry prelude that the voucher policy cannot accept. The policy requires the authenticated spent receive and both user and reference tokens atomically. The Gateway now constructs that atomic transaction; the former prelude builder and relayer path explicitly reject it.

The original compiled atomic receive evaluated successfully script-by-script but exceeded the actual 16,500,000 transaction memory limit: **22,350,459 memory units**, seven executions, 10,181 unsigned bytes. No oversized receive was submitted or counted as accepted. The first-seen voucher path on that immutable deployed baseline remains blocked by execution cost.

Semantics-preserving optimizations remove repeated decoding/traversal, combine old/new commitment-root calculation, and use exact lowercase hex lookup. Offline evaluation of the same captured context with candidate script overrides measures **16,015,944 memory / 5,215,362,331 CPU units**. Overrides are diagnostic only: they do not show that the original scripts or a node accepted replacement code. This measurement covers the small captured state; populated archives, rollover, larger packet profiles, cold history, and near-capacity channel history remain separate budget requirements.

The first optimized-baseline deployment attempt accepted its registry/HostState and reference publications but stopped before minting the transfer module. Ogmios v6.12.0's explicit JSON script decoder uses `ledgerLanguageIntroducedIn` rather than the node's active protocol, rejecting newly enabled builtins. A probe reproduced the rejection for the candidate HostState/kernel while accepting their exact bytes using Ogmios's supported tagged-CBOR script representation. Both off-chain providers now use that encoding; ledger evaluation remains required. The incomplete fixture is retained separately and is not a populated bridge or successful migration. [Ogmios decoder source](https://github.com/CardanoSolutions/ogmios/blob/v6.12.0/server/src/Ogmios/Data/Json/Query.hs#L2228).

The next attempt, with the same compiled blueprint and corrected provider encoding, completed on the unchanged protocol-10 five-pool devnet: 30 reference scripts, 57 measured transactions, and a largest signed transaction of 16,192 bytes against the 16,384-byte limit. Its transfer-module binding was accepted as `a6967529688c10466e4d2126d790e23fd9daa91a3c5b1c0619efd148dcb4e11a`. The artifacts are isolated in `.deployment-smoke/optimized-baseline-v1-20260917-rpc`. This establishes deployment feasibility, not populated migration acceptance.

On that distinct optimized baseline, the real handshake opened Cardano `channel-0/1` to Cosmos `channel-2/3` over `connection-0/1`, using Cosmos client `08-cardano-probabilistic-1`. Cardano receive `f3cf1530061d8a828d357af0a5188b1173b723fc9b36321e408e522e06b81403` (block 2365) issued 1,000,000 units of the exact foreign voucher `a0a4e31a378aa4feda67643132fbc25c83f39632652108912f8cc8da0014df10147df96de042ea2e25dd031396f26040eef3a0d1e0dd9cb59ec88724`, alongside its required metadata reference token. Its seven redeemers consumed **15,717,509 memory / 5,137,271,443 CPU units**, below the actual 16,500,000 / 10,000,000,000 limits. Fee: 4,185,368 lovelace; reconstructed body/witness transaction encoding: 10,435 bytes. The first epoch-2 heartbeat, `c9f936c3e5068e0e2d2e3035fb2a310b17455da14ee04d35fc2e2195b7f77719`, supplied a new accepted anchor after the receive occurred too near the previous epoch boundary. A stale-wallet heartbeat attempt correctly failed signing; lazy construction now captures refreshed inputs inside the completion lock.

Native fixture asset A was escrowed by `0bec4d978902f7e7d7fa5e893c6cf32fd1837646d8d1d807ba1c4efc035f364f` (block 2701), amount 10,000, channel-0 sequence 1. The real counterparty accepted its receive and success acknowledgement at Cosmos height 2192. Its Cardano acknowledgement remains deliberately pending. A second send attempted before stability readiness failed at querying and did not broadcast; this is not an accepted transfer.

Independent continuity review found and prompted fixes for exact packet-root checking, delayed Cardano proof heights, historical queries gated by unavailable live state, rollback-stale historical NFT projections, historical cold startup, and HTTP builder bypass of recovery mode. The focused recovery boundary suite passed 26 tests with real PostgreSQL canonical-history cases and actual Nest HTTP dispatch; the exact-root packet regression passed separately. The first full Gateway rerun found one existing mock asserting the former SQL column spelling. After updating that expectation, the entire suite passed: **138 suites / 1,132 tests, zero skipped**, including the real PostgreSQL cases and 16 migration/Mithril startup matrix cases. The review does not establish populated migration continuity, Mithril historical certification, or independent journal review.

The second Cardano holder received 500,000 stake vouchers on channel-1 in `28bcdbdf58c04000e02ec167fb92f5d5ef1eafcdceee247658b8cdfc1706867e` (block 2828): 16,099,247 memory / 5,253,148,318 CPU units. Native asset B was escrowed in `4fdf23762bd5eecf5c82678c0a63dbb4c4f750d7c94aa3a1a27c47763dc1de9b` and received by Cosmos at height 2223. The fixture also contains native sends awaiting timeout and receive, foreign receives queued on Cosmos, and a 2,000-unit return against the original native A escrow.

A fixture error sent 100,000 foreign vouchers by raw Cardano asset unit in `5b9fce79b9a42e3d34c1b309b5eec1cebb99a9c58f1f07e2d8b2e9d8e9996810`, selecting ordinary escrow, not burn. This remains an extra timeout/refund obligation; it is not evidence of a burned return and is not a migration solution. The corrected send uses `transfer/channel-0/stake`: `435b5c3f68f7e61beb4d0e284a257dfff68094066e53823a6dc5aab8511b8b28` (block 2960) **burned 100,000 under the exact original voucher unit**, measured directly from the canonical transaction mint map. This packet is deliberately left for timeout/remint after migration. Its execution used 9,559,151 memory / 3,003,844,665 CPU units.

Read-only helpers `measure-migration-transaction.cjs`, `capture-migration-population.ts`, and `verify-migration-population.ts` record canonical inclusion/budgets and compare authenticated state identities, raw datums, assets, holder balances, and an independently recomputed port commitment transition. Snapshot comparison does not prove finality or later packet settlement. The final source rebuilt to exactly the deployed optimized blueprint digest; the old deployment fixtures remain unchanged.

The populated bridge snapshot contains nine authenticated objects and two holders. V2 reference publication and governance approval completed; approval transaction `b508fd14c8a28e30f1320e4f4b93c6d47663c5c5fe522f3d6deea7c93d553222` is 2,451 signed bytes with a 656,479-lovelace fee. The approved canonical plan digest is `f67644260d58a1f5ffe1feac46cdeb6bb53b50b02d92fdfea2cc378583871938`. It retains the 86,400,000-ms governance delay. A premature secondary-wallet execution failed CLI preflight; that is not a live validator rejection. After the full delay, a separate executor with no governance key moved HostState in `962fbd4a774b15637c2b336a4d545e7f376e807191e57945d569bbfb3b1bdbf1`, exited, and resumed in a new process. Eight subsequent one-object transactions moved the client, connection, two channels, transfer root and three escrow shards. Activation `13617027e557f874e74de63540a7c159a243ae39bbd16ce56a0a681d85543d64` was accepted at Cardano block 3517. Independent before/after snapshot comparison passed for all nine objects and both holders: exact non-ADA identities/principal, nondecreasing deposit ADA, preserved non-Host datums, and the independently recomputed transfer-port root transition. This is canonical local-ledger evidence, not a public-network finality certificate.

The disposable clock rehearsal initially attempted an excessive jump and stalled block production. All five Cardano tips remained at block 3072; recovery preserved that canonical block and restarted producers ahead of it, leaving already advanced Cosmos timestamps unchanged. Subsequent bounded steps capture actual ledger stake and require complete canonical epoch-nonce history. A shared producer offset file avoids restarting every pool at every step. This changes only isolated process clocks, never host time, genesis, script delay, or verification rules. Clock progress is test infrastructure, not migration-finality evidence.

The first bounded clock helper checked only the main producer. Post-activation stability correctly refused new proofs: the four peers were still on an epoch-3 fork. Restart alone did not synchronize them. Their unmodified databases and pre-recovery tips are retained; the peers replayed the main node’s existing chain from genesis. No block, genesis, key, or verification rule was edited. The helper now requires every producer’s advanced-window tip to be present in canonical history before each further clock step. This infrastructure recovery is not evidence of public-network consensus recovery or a migration rollback.

The ten actual V1→V2 execution transactions (Begin, eight object moves, Activate) minted no assets. Their total fees were **12,737,765 lovelace**, paid by the external executor. Maximum reconstructed signed size: **4,292 bytes**; memory: **2,627,728**; CPU: **845,332,215**. Approval cost another 656,479 lovelace. Successor reference publication cost 6,418,677 lovelace in fees and locked 285,528,880 lovelace in new reference deposits. Original principal and infrastructure ADA were conserved separately.

After peer recovery, current-root readiness at height 3517 passed. The existing counterparty client accepted rootless checkpoints through height 3068 but could not cross the sparse epoch-3 history: the next candidate lacked even the required 24 same-epoch descendants. Its original accepted root at 2749 and earlier acknowledgement acceptance remain intact. This attempt therefore does **not** establish post-migration counterparty continuity. A new separately retained rehearsal uses a fresh genesis with 432,000-slot epochs and all-producer clock checks; the failed deployment/client are not reset or reinterpreted as a successful run.

Independent review strengthened `migration_packet_model.test.ak`: its event-log oracle now identifies old packet IDs and forces acknowledgement, timeout and pruning after each of two authenticated `MoveCore` handovers. It checks independent commitment encoding against production hashing and exercises all four preserved-datum mutation classes. This is compositional unordered-channel state coverage; activation is assumed, and ordinary packet policies, counterparty proofs, transfer accounting, mint/burn/remint and balanced-ledger acceptance remain outside that property. Its aggregate test execution cost is not a transaction budget.

Historical-proof preparation exposed a production Gateway wire bug: single commitment and raw acknowledgement responses supplied hex strings to protobuf `bytes` fields; both paginated hash lists also decoded stored hex as base64. Five regressions using the real configured protobuf serializer failed with corrupted payloads before the fix and passed afterward. Responses now decode the datum's hex into actual bytes. The complete Gateway rerun passed 138 suites / 1,158 tests with the real PostgreSQL history cases, including exact accepted-anchor handling. A subsequent typed migration-pause error passed the 20-test proof-context suite and the Gateway build.

A Gateway initialized against a fresh empty cache database served the old channel/acknowledgement proof at Cardano height 2749 while the registry was Moving; ordinary readiness returned 503. Actual gRPC negative controls rejected current packet-list queries and heartbeat building with `FAILED_PRECONDITION`. After activation, the existing Cosmos client `08-cardano-probabilistic-1` accepted that exact historical proof and original packet in transaction `824EC0EA67244DCFF4FBC490960312748766EEA72DD03FF190D4EBA72A9314AF`, Cosmos height 3469, code 0, gas 105,687. No new client or route was installed. A second signed submission of the exact message was rejected by ibc-go v8 CheckTx with `channel/22: packet messages are redundant`; the receipt binds it to the first successful delivery. Current-root successor proofs, the remaining claims, and V2→V3 are still being exercised.

The replacement V2 Tendermint client accepted actual updates, and Cardano timeout transaction `71031b4af88c5f110406de5f564f7d08d82828e3457497b25b53b0a9b08866c5` (block 3723) reminted exactly 100,000 units under the original voucher policy/name from the pre-migration burned return. Its canonical mint map, 15,148,946 memory units, 4,996,225,157 CPU units, 8,714 reconstructed bytes and 3,766,543-lovelace fee are retained. This confirms that specific original-policy remint path despite the separate counterparty epoch-history failure.

The same V2 accepted the pre-migration native return in `0139297ffa018b67c2d8f4d68393f9efdf2908ff98d90c8f31ade2cd739296fb`, block 3779. The observed holder balance increased by exactly 2,000 original native units and its original escrow decreased from 11,000 to 9,000. Canonical measurement: 13,634,083 memory units, 4,326,464,078 CPU units, 10,020 reconstructed bytes, and 3,349,591 lovelace. This is specific redemption evidence; the old run's counterparty-history failure remains unresolved and disqualifies that run from complete acceptance.

A separate five-day-epoch baseline completed under `.deployment-smoke/long-epoch-baseline-v1-recovered`, using the unchanged optimized baseline blueprint and protocol-10 limits: 57 transactions, 30 references, maximum signed size 16,192 bytes. The earlier partial publication and funding attempts are retained. Local VM exhaustion stopped all producers beyond their forecasting window; before any complete new baseline or counterparty existed, their disposable clocks were returned near the last canonical tip. `clock-resource-recovery.json` records that recovery; no committed blocks, genesis or approvals were rewritten. The old V2 deployment is stopped with all data retained. The new chain has actual first-header replay, epoch-zero ledger stake evidence, and a fresh counterparty. Populated V1→V2→V3 acceptance is still pending.

That fresh baseline completed all 13 population operations through the existing `connection-0`, `channel-0/1` routes and Cosmos client `08-cardano-probabilistic-0`. Its quiescent snapshot contains eight authenticated state/escrow objects, two native fixture assets, two holders, remote native vouchers, and foreign voucher obligations. Cardano transaction `93470b049b50722a52fac8322b174665002d0f5ed7575ac1dd9c430d6c7dd88a` actually burned 100,000 of the original foreign voucher for the pending timeout/remint case. All 13 receipts were revalidated against canonical packet events/redeemers and complete raw block-body/witness commitments. The independent fixed-flow oracle passed, including both original Cosmos escrow accounts. The counterparty accepted the pre-migration root at Cardano height 924 without a new route.

The fresh V2 plan has canonical JSON SHA-256 `ae46aec3027779d14c00efa18ecbd5cea6220a3818060bff30e593885b22b93d`; approval `64b446d1c32f23a6e6cfbd53ad80a29e17d1d5a5ccf59195a969a2bbbf5d4686` retained the full 24-hour delay. Approval consumed 644,371 lovelace in fees and added 1,344,720 in registry deposits, exactly matching the authority wallet's 1,989,091-lovelace debit. An earlier prepared artifact was rejected before submission because its source state had changed. A separate ADA-only executor, with neither holder payment key nor governance authority, is funded for custody execution. Governance and reference publication are separate costs; the public approval fixture also serves as the primary holder, so the approval transaction itself is not holder-signature independence evidence.

After that full delay, Begin `c18d4506d42e7b0558569a9be7cc96c4b99bd76d07b2e9153c72151176628fe0` established Moving and the execution process exited. A Gateway with a unique empty cache served the original accepted channel/acknowledgement root at height 924 while readiness stayed 503; real gRPC current packet queries and heartbeat construction returned `FAILED_PRECONDITION`. A separate resumed process completed the remaining moves. Activation `df313e501ac155ae1e5fd12a1f2ef3595759ba67f2f4be205d4d389ff8b86b02` was accepted at Cardano block 1633. All eight objects and both holders passed independent state/asset conservation and the exact port commitment transition. Canonical measurements bind all nine custody transactions through the registry input/output chain: only the separate executor signed, nothing was minted/burned, fees totaled **11,320,132 lovelace**, infrastructure increased **676,670**, and the executor debit was exactly **11,996,802**. Maximum reconstructed signed size was **4,100 bytes**, memory **2,601,866**, and CPU **836,287,713**. The new generation/identity/role binding check also passed on these actual snapshots. Post-V2 packet settlement and V3 remain pending.

The existing Cosmos client then accepted the post-V2 Cardano root at height 1744. Transaction `F2C15EC092BD6EA931ECD8866E0468DC2CDBA85A2E5076F0BD661097BC62C697`, Cosmos height 1142, acknowledged the original foreign packet on the original channel using that root. Its actual `MsgAcknowledgement` and CometBFT data commitment were verified. Receipt capture initially failed because ibc-go v8 acknowledgement events omit packet payloads; the verifier now binds the full body using the event's exact message index. The retained successful log was reconciled without resubmitting the accepted packet. Both original native acknowledgements also passed canonical packet checks on Cardano under V2; full V2 settlement and V3 are still pending.

The fresh V2 then accepted native timeout refund `e2eceb6dae38539088b0c71de075a98e82299d31c3dba58f020a38507852c9c1` and burned-voucher timeout/remint `00761c11c6ce4f9cd1bac4fdf0cbec0ef5f7fb48d59929cd029f9a16974638e6`. The latter canonically minted exactly 100,000 units of original voucher `cf0321bfec37397dda2615e20eded450510409e0d7092b69a52bdba30014df10147df96de042ea2e25dd031396f26040eef3a0d1e0dd9cb59ec88724`; it used 14,814,686 memory units, 4,878,918,130 CPU units, 8,408 reconstructed bytes and 3,725,334 lovelace. Its first attempt failed querying application status before construction when Yaci lacked a block witness and the fixture omitted node fallback configuration. The failed log remains intact; an explicitly selected retry retained a separate log and revalidated the original send. The pending native receive completed on Cosmos at height 1404. Pending foreign receive `e29fcd56aa8834fd9ec68c21025ce38afb1afa0ebabeea53097d3f839b15c919` and native return redemption `cf2af96a2f65d7ddbb75466726c8501da6d1be6220b868bed332df63bc6feea0` also passed exact canonical packet checks under V2. The completed V2 settlement snapshot then passed the independent holder/backing oracle: original native A backing 8,333, native B backing 7,900, original primary foreign voucher/remote backing 1,118,456, and secondary foreign voucher/backing 500,000. New native send `2b6fd1b3a452c8ec1f78980fa2fd469829ca10d053a941bd4a8d91933f585c57` delivered 333 units on Cosmos at height 1520. New original-voucher return `903c402261a517d5cf3beddd3ac1a3d06d912fedf40e2695de55e88b34584550` burned 5,000 and redeemed on Cosmos at height 1547. All thirteen V2 settlement steps have canonical receipts; five acknowledgements deliberately remain for V3. The native-return transaction measured 13,669,060 memory, 4,336,319,577 CPU, 9,971 reconstructed bytes and 3,350,164 lovelace. V3's first catch-up command stopped because Hermes returns an error when the existing client is already current; no migration transaction had been attempted. The driver now checks actual accepted/client heights and historical proof availability before requesting an update or preparing approval. The retained failure was resumed from `approve-v3`, preserving all V2 evidence.

Independent review of the accounting and rehearsal drivers led to payment-credential checks (including alternate addresses for the same key), an exact registry-input/output chain across every custody transaction, per-object infrastructure conservation, bootstrap-witness rejection, canonical funding revalidation, unique cold-cache names, pre-Begin HTTP/gRPC port checks, generation/deployment/role binding that rejects a V2 bundle relabeled as V3, and retention of public reproducibility inputs. A golden real block control rejects a witness mutation that leaves its header and transaction-body hash unchanged. The repeatable driver and manual CI workflow are implemented; the full V1→V2→V3 driver has not completed. Remote CI run [35238366088](https://github.com/cardano-foundation/cardano-ibc-incubator/actions/runs/35238366088) is executing the complete fresh rehearsal. Its unused-export ratchet exposed an obsolete exception for `requireSttDeploymentConfig`, now used by migration code; that exception was removed and the local ratchet passed. The latest requested journal review was tool-blocked and remains an explicit independent-review gap.

Independent review of the rehearsal verifiers found and led to fixes for endpoint/genesis mismatch, aggregate-only escrow checks, assertions disabled by Python optimization, ambiguous packet state transitions, and wall-clock timeout assumptions. Packet evidence now decodes the canonical channel spending redeemer and checks its complete packet and operation alongside independently calculated commitments and exact state transitions. Timeout intent uses destination application timestamps. The bounded follow-up review found no remaining concrete defect and checked retained CBOR offline; it did not establish live acceptance or provide an audit.

An actual Ogmios handshake timeout crashed Gateway because timeout cleanup removed its error listener before terminating a connecting WebSocket. A regression using a real TCP listener that never upgrades HTTP failed with that unhandled error. The fix retains a cleanup listener through close; all 34 transport/Ogmios tests and the Gateway build passed.

The original unapproved V3 rehearsal fixture imposed a five-minute normal-operation window, incompatible with the existing nine-minute staged Tendermint update builder. It remains retained. A separate V3 fixture uses a fifteen-minute window (blueprint SHA-256 `59e9fe42a82a18141a8a902ee605a39624494b7f77ea6ec072e50e3f7a1851ac`); V2's already approved ten-minute fixture is unchanged. Both releases reject the broad two-hour heartbeat accepted by the baseline in compiled transaction tests. These are explicit test releases, not a production governance recommendation.

`measure-migration-scaling.ts` evaluated 15 signed one-object steps at the retained local protocol-10 profile. Channels cover 0/32/64 pending commitments and seeded authenticated limits/cursors up to 1,000 channels; escrow inventories cover 2/32/1,024 shards with independently recomputed roots and 64 siblings. Maximum signed size: **4,249 bytes**; maximum memory: **2,622,275**; maximum CPU: **847,752,944**; maximum fee: **1,325,694 lovelace**. These are seeded offline fixtures, not execution of all preceding channel moves or ledger creation of the large populations. The active V3 cost model is exact; ten unused V2 sentinel entries are normalized for Lucid and explicitly recorded. The original parameter file is retained as `cardano/offchain/src/testing/protocol-10-local-cost-profile.json`; CI evaluates and exports the same bounded scenarios.

Fresh V3 authorization `e19b5e27748b110b1fd4e778a8d1f5ec9e7d3204607a8261f96f69a8aa9e32a2` binds generation 2, nonce 2 and canonical plan digest `2de749937553b9182e8a8cbf222c94a6f742c5e6f81226bf83b2969a8603a70e`. It measured 2,587 bytes, 1,012,837 memory units, 408,402,005 CPU units and 659,331 lovelace. The existing registry deposit was sufficient; the authority wallet's ADA debit was exactly that fee. The approved ready time is 1767148090000 ms, with the immutable 86,400,000 ms delay intact. All five V3 references are published. The handover is pending completion of the isolated clock advance, which checks all five producers against canonical history after each bounded step.

Fresh V2's five reference publications and their five funding transactions were independently measured from canonical block bodies: 7,294,626 lovelace in fees, 285,528,880 in reference deposits, and maximum reconstructed transaction size 16,067 bytes. These are additional to approval and custody costs, not withdrawals from escrow principal. `publication-costs-v2.json` lists the exact ten transactions and `v2-publication-measurements/` retains their measurements. V3's corresponding ten canonical transactions cost 7,333,830 lovelace in fees and the same 285,528,880 in reference deposits; its maximum reconstructed size was 16,069 bytes. See `publication-costs-v3.json` and `v3-publication-measurements/`.

The forced node-witness fallback probe exposed an existing transport bug: the library's default handshake requests only the peer's version table, then block fetch waits indefinitely. Gateway now explicitly negotiates a non-query session, validates acceptance/network magic, normalizes the node's era envelope, bounds HTTP/handshake/fetch waits, and handles protocol-client errors. Independent review found and led to fixes for half-open socket cleanup and unhandled client errors; bounded re-review found no remaining concrete defect. With Yaci disabled, the actual node returned the native-refund block at slot 93085; independent complete header/body authentication passed on 9,210 bytes. A real silent local TCP peer triggered the 30-second deadline and observed socket closure after 30,109 ms. These are transport checks, not additional migration or consensus evidence. Reports are `node-witness-fallback.json` and `node-witness-timeout.json` in the fresh rehearsal artifacts.

## Recent executable checks

Commands run from the feature worktree unless a directory is named:

| Check | Actual result |
| --- | --- |
| `cardano/onchain`: `aiken check -m migration_budget_equivalence -m voucher -m trace_registry --seed 462 --max-success 1000` | 98 passed: 59 unit tests and 39 properties, each property configured for 1,000 successes. Independent scalar metadata encoding, declarative directory constraints, and independent Merkle-root traversal are included. |
| `deno test -A --config cardano/offchain/deno.json cardano/offchain/scripts/migrate-deployment.test.ts` | 6 passed: concurrent expired rebuilds preserve one immutable winner, plus argument constraints, lost RPC response, concurrent executors, expiry with original-input pinning/substitution rejection, and canonical adoption of an older revision after a simulated rollback. Journal isolation tests use real Emulator transactions; this is not a chain rollback rehearsal. |
| `cardano/gateway`: `npm test -- --runInBand lucid.service.trace-registry-prelude.spec.ts packet.service.denom-regression.spec.ts` | 8 passed. These are focused builder/service regressions, not on-chain lifecycle evidence. |
| `relayer`: `cargo test -p ibc-relayer --lib chain::cardano::signing_policy -- --nocapture` | 43 passed, including outbound refund sender normalization and separate signer authentication. |
| `relayer`: `cargo test -p ibc-relayer --lib trace_registry_prelude_is_rejected -- --nocapture` | 1 passed. An otherwise valid single receive cannot authorize an unsupported prelude. |
| Full Gateway suite with `BRIDGE_HISTORY_TEST_DATABASE_URL` pointing to the isolated test database | Final witness-fix rerun: 140 suites, 1,170 tests passed, zero skipped; includes real PostgreSQL history tests and the accepted-anchor, wire-encoding, migration-pause, WebSocket timeout and node-witness regressions. |
| Gateway witness transport/header focused tests | 15 passed; actual node and real silent-peer probes also passed as described above. |
| Optimized-blueprint migration suite with distinct compiled V2/V3 fixtures | 21 tests and 18 steps passed, rerun with the compatible fifteen-minute V3 fixture; Emulator population is not a substitute for the real two-chain rehearsal. |
| SDK runtime `npm run build && npm test` | 91 passed, zero skipped; a burn-builder test was updated to await the now-asynchronous registry lookup. |
| Relayer `cargo test -p ibc-relayer --lib` | Final rerun: 275 passed, zero skipped, including the delayed scheduling regression. |
| Relayer `cargo fmt --all -- --check` and `cargo +1.85.0 clippy -p ibc-relayer --lib --tests -- -D warnings` | Passed with the pinned CI toolchain. The initial local default Rust 1.91 Clippy invocation failed on an unchanged manual `Default` implementation in `relayer-types`; no lint was suppressed. |
| Relayer delayed scheduling regression through real runtime request/reply boundary | Passed in both relay directions: Cardano proof height 33 remains 33 even with a checkpoint cursor at 50. A non-packet fixture initially did not enter the delayed branch and was corrected to an actual packet event; assertions were retained. |
| `aiken check --deny -m migration_packet_ --seed 462 --max-success 1000 --property-coverage relative-to-tests` | 6 passed: 4 unit tests and 2 properties with 1,000 iterations each. Claim mutations covered commitment (260), receipt (260), acknowledgement (247), and proof-floor (233) cases. Seed 462 reproduces the run. |
| `python3 scripts/ci/test-migration-guards.py --report …` | 4 guard controls, each evaluated in production and a temporary mutant: 16 full-validator checks passed. Quorum, principal conservation, core completeness and packet-datum preservation each reject the same attack that becomes accepted with only that guard removed. |
| `aiken check --deny -m migration_adversarial_cleanup --seed 462 --max-success 100` | 3 passed: the exact deployer-signed HostState shutdown passes retained legacy rules and current registry authentication, then the upgrade-capable wrapper rejects it in both Ready and Proposed. These isolated validator controls do not claim a legacy deployment migration or balanced-ledger shutdown acceptance. |
| `deno check --config cardano/offchain/deno.json cardano/offchain/scripts/migrate-deployment.ts scripts/ci/export-migration-budget.ts scripts/ci/migration-cost-overrides.ts` | Passed. |
| Off-chain `deno task fmt:check`, `deno task lint`, `deno task check` | Passed. Journal tests reran after lint-only Promise-return fixes: 6 passed. |
| Repository invariant, lint-suppression, Aiken layering and fuzz-import checks | Passed. |
| `python3 scripts/ci/test-migration-evidence.py` and `node --test scripts/ci/test-migration-packet-evidence.cjs` | 12 Python and 9 Node tests passed, including optimized-Python rejection, wrong shard/principal/provenance, changed receive payload, acknowledgement/timeout ambiguity, exact Cosmos packet events and indexed message bodies, raw witness/body authentication, occupied-port preflight, and external-payer/signature/deposit accounting controls. Added to Gateway CI. |

Prior compiled migration, model, guard-removal, Gateway history, and relayer integration checks exist, but must be reconciled against the final source/artifacts before release. Remote CI is in progress; no full remote success is claimed. The whole-project optimized-source rerun (`aiken check --seed 462 --max-success 100`) passed all 1,440 tests: 1,284 unit tests and 156 properties.

## Requirement mapping and remaining acceptance work

| Requirement | Implementation / executable coverage | Remaining evidence |
| --- | --- | --- |
| Exact approval, delay, rotation, cancellation, source/nonce binding | `implementation_registry.ak`, `minting_implementation_registry.ak`, `lib/ibc/migration`, `migration_adversarial.test.ak` | Final compiled regressions, real delayed approvals and repeated migration |
| Real state and escrow handover with authenticated completeness | Upgradeable wrappers, registry cursors, escrow remaining-root transitions, `migration-transactions.ts`, `migration.test.ts` | Populated native/foreign assets and all packet obligations through V1→V2→V3 |
| Stable policy/asset identity and compatible successor artifacts | `migration-plan.ts`, `migration-manifest.ts`, original applied script retention | Exact pre-existing voucher redemption, burned-return remint, and new transfers after both activations |
| Crash/concurrency/expiry recovery | `migration-submission.ts`, `migration-operations.ts`, CLI journal tests | Operational interruption/rollback rehearsal; independent review of latest journal revisions |
| Gateway/history/counterparty continuity | Runtime migration references, history lineage/replay, Gateway manifest validation, Hermes strict profile and proof-root handling | Real proofs spanning each migration; cold history and historical settlement |
| Cleanup guards | Retirement-disabled upgradeable validators, shutdown refusal | Final compiled negative controls and operational premature-cleanup checks |
| Bounded costs and CI | Per-step transaction construction, diagnostic budget exporter, existing CI migration job | Actual node budgets/deposits, populated/scaling cases, deterministic complete rehearsal in CI |

The independent optimization review found no accepted-input security relaxation in the inspected changes; it specifically called for actual protocol-cost-model validation and larger-state measurements. A local review or rehearsal is not an independent audit. Arbitrary approved successor code, faults in immutable policies/kernel, lost authority, or irrecoverable light clients remain outside the recovery guarantee.
