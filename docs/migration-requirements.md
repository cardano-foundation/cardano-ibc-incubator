# Migration requirement-to-evidence map

**Evidence version:** populated results below describe the earlier seven-field registry profile. The emergency-capable profile v2 changes the kernel and wrapper ABI; its populated lifecycle, real rollback and network-scale downtime must be qualified separately. Do not read historical passes as acceptance of the current candidate.

This map concerns compatible replacement of a fresh upgrade-capable baseline. It is not a claim that an existing immutable deployment can migrate. The [design](state-preserving-redeployment.md), [operator runbook](migration-operator-runbook.md), and [evidence ledger](migration-implementation-evidence.md) define the supported boundary and distinguish completed checks from pending work.

| Requirement | Enforcement / operational code | Executable evidence and limits |
| --- | --- | --- |
| Immediately contain a newly discovered ordinary-path exploit | **Missing:** no registry restriction field/redeemer; proposal permits normal operations and the old shutdown is disabled | [Containment audit](emergency-containment-audit.md): 44 new compiled cases, included in 243 passing regressions, plus a phase-guard removal control. A separate restriction authority/design is proposed but not implemented; the migration delay cannot be bypassed to simulate an emergency stop. |
| Establish the baseline and preserve unrelated work | Starting main commit `fde2635138f60f6d14954b2f91e4a001b74c12a2`; isolated `feat/462-state-preserving-redeployment` worktree | The original dirty worktree remains separate. The evidence ledger records distinct initial, optimized, failed and current deployment fixtures. |
| Distinguish immutable deployments, the new baseline and successors | [Deployment planner](../cardano/offchain/src/deployment-plan.ts), [migration plan](../cardano/offchain/src/migration-plan.ts), retained legacy validator entry points | No accepted migration of an original immutable deployment is claimed. New fixed policies are deployed only as part of a fresh baseline. Successors retain those exact applied policies. |
| Bind approval to the source, nonce, complete successor role map and compatibility | [Registry validator](../cardano/onchain/validators/implementation_registry.ak), [migration types](../cardano/onchain/lib/ibc/migration/types.ak), [authentication](../cardano/onchain/lib/ibc/migration/auth.ak) | Compiled adversarial controls cover unauthorized/substituted successors, wrong identity and stale proposals. Actual V2 and V3 approvals bind distinct complete plans. |
| Authenticate the registry itself | [One-shot registry policy](../cardano/onchain/validators/minting_implementation_registry.ak), migration authentication and original deployment inputs | Actual baseline deployment mints the registry identity; compiled tests reject counterfeit registry/state tokens. Possession of look-alike datums does not authorize continuation. |
| Enforce delayed activation, cancellation and authority rotation | Registry validator; [operations](../cardano/offchain/src/migration-operations.ts) and [CLI](../cardano/offchain/scripts/migrate-deployment.ts) | Compiled tests exercise delay/cancellation/rotation. Both V2 and V3 completed the immutable 24-hour delay. No default production governance keys are installed. |
| Separate code approval from execution | Registry phase rules; [transaction construction](../cardano/offchain/src/migration-transactions.ts) | All eighteen actual V2/V3 custody transactions have only the separate ADA-only executor's signature. Reference publication and governance approval have separate funding and signing evidence. |
| Authorize exact successor credentials | Full Plutus addresses in the signed proposal and migration continuation checks | Adversarial tests cover redirection and address substitution. The rehearsal verifier also compares payment and staking credentials, not just displayed addresses or filenames. |
| Preserve voucher, state-token, route and application identities | Fixed applied policy inventory; [upgradeable validators](../cardano/onchain/validators/upgradeable/); [voucher policy](../cardano/onchain/validators/minting_voucher.ak) | V2 redeemed and reminted original voucher assets, with both original channels and the original Cosmos client. The exact policy IDs, asset names and balances appear in canonical receipts and population snapshots. V3 settled the five carried acknowledgements and new traffic in both directions; all 37 receipts passed final canonical revalidation. |
| Replace actual escrow and channel spending logic | Upgradeable HostState/client/connection/channel/transfer spenders and registry custody steps | All five V2 spending hashes differ from V1. Eight live objects, including two escrow shards, moved in actual ledger transactions. V3 moved those same eight objects to five further distinct spending hashes. |
| Preserve principal, balances and unique continuation | Registry continuation rules, full source datum/value/reference-script checks | [Compiled adversarial tests](../cardano/onchain/validators/migration_adversarial.test.ak), [population comparator](../scripts/ci/verify-migration-population.ts), and [independent funding verifier](../scripts/ci/verify-migration-funding.py) reject diverted principal, duplicated/omitted objects and shared-output satisfaction. Actual V2 and V3 principal and holder balances passed. |
| Preserve commitments and verify the permitted committed change | [IBC commitments](../cardano/onchain/lib/ibc/core/ics-025-handler-interface/ibc_state_commitment.ak), registry activation, [witness exporter](../cardano/gateway/src/scripts/export-migration-witness.ts) | Activation verifies the exact transfer-port leaf transition. Actual V2 and V3 root/state comparisons passed; sequence and packet-map mutations have compiled rejection controls. An operator-provided replacement root is never sufficient. |
| Prove completeness across bounded transactions | Sequential authenticated core cursors and authenticated remaining escrow inventory | Each actual handover used nine custody transactions and an interrupted/resumed process. Indexer scans cannot satisfy missing cursors or an unemptied remaining root. Compiled scaling contexts cover larger inventories; a thousand-object ledger rehearsal is not claimed. |
| Define the mixed-version boundary | Registry phases and normal-dispatch guards; [runtime integration](../packages/cardano-ibc-tx-builder-runtime/src/migrationRuntime.ts) | Approved freezes new core/shard creation. Moving freezes ordinary HostState changes in both generations: send, receive, return, acknowledgement, timeout, refund, pruning and final client application. Fixed session verification/cancellation remains available. No ordinary mixed-version packet service is supported. |
| Survive interruption, rejection and competing execution | [Submission journal](../cardano/offchain/src/migration-submission.ts), operations and canonical registry reads | Real V2 process exit after Begin and separate-process resume passed. Journal tests cover lost responses, concurrent executors and expired revisions anchored to the same input. Canonical adoption of an older revision is tested in Emulator; an actual chain rollback rehearsal remains outstanding. |
| Provide safe cancellation and protect outstanding dependencies | Registry cancellation boundary, upgradeable shutdown guards and [shutdown tooling](../cardano/offchain/src/shutdown.ts) | Cancellation is available only before Begin. Three compiled cleanup controls show the same valid legacy shutdown rejected by the upgradeable wrapper in Ready and Proposed. Terminal retirement is disabled; no rollback after Begin or successor activity is promised. |
| Preserve counterparty proof continuity | Historical lineage/replay, [proof context](../cardano/gateway/src/query/services/proof-context.ts), Hermes manifest/signing checks and retained proof heights | The original Cosmos client accepted the post-V2 root at Cardano height 1744 and acknowledged an old packet. A cold Gateway also served the pre-migration accepted root during Moving while real gRPC builders/current queries rejected operations. The same client accepted post-V3 proof height 3385, after activation at 3040, to acknowledge another old packet. |
| Reject unsupported counterparties/configurations | Gateway manifest validation, migration compatibility profile and Hermes pinned manifest | The supported path is ibc-go v8 classic ICS-20 with the probabilistic Cardano client. Migration plus Mithril is rejected, including historical-only mode. Existing channel-upgrade support is not assumed or emulated. |
| Integrate preparation, authorization, execution, recovery and verification | Migration CLI, deployment planner/manifests, Gateway/SDK and Hermes | The actual V2 run used production builders and installed a verified successor manifest. Commands and interruption rules are in the runbook. A missing or incompatible artifact fails closed instead of creating another bridge. |
| Settle pre-migration obligations and allow new traffic | Original packet policies, new active spenders, real Gateway/Hermes/ibc-go transactions | V2 settled old receives, acknowledgements, native timeout/refund, burned-voucher timeout/remint and native redemption, then delivered new native and foreign-return traffic. All 13 V2 settlement steps have canonical receipts. All five acknowledgements intentionally carried across V3, new traffic in both directions and final canonical revalidation passed. |
| Independently check lifecycle and accounting | [Packet state-machine properties](../cardano/onchain/validators/migration_packet_model.test.ak), [traffic oracle](../scripts/ci/verify-migration-traffic-balances.py), funding verifier | Fixed-seed properties use an independent event-log/commitment oracle and meaningful transition labels. They compose migration with packet-map behavior; they are not a full balanced-ledger random asset model. Independent holder/backing arithmetic and the real two-chain run supply separate accounting evidence. |
| Prove negative controls target the intended invariant | [Guard-removal controls](../scripts/ci/test-migration-guards.py), compiled adversarial tests and [packet evidence tests](../scripts/ci/test-migration-packet-evidence.cjs) | Four guard controls perform 16 production/mutant validator checks for authority, principal, completeness and packet datum preservation. Cleanup controls first establish valid legacy authorization. Canonical-block and packet-body controls reject altered witnesses, payloads and ambiguous transitions. |
| Measure fees, deposits, execution budgets and scaling | [Canonical transaction measurements](../scripts/ci/measure-migration-transaction.cjs), [scaling measurements](../scripts/ci/measure-migration-scaling.ts) | Actual V2 custody: 11,320,132 lovelace fees, 676,670 added deposits, maximum 4,100 reconstructed bytes / 2,601,866 memory / 836,287,713 CPU. Publication and approval costs are measured separately. Real voucher remint also fits unchanged protocol-10 limits. |
| Make evidence reproducible in CI | [Main CI](../.github/workflows/ci.yml), [fresh rehearsal workflow](../.github/workflows/migration-rehearsal.yml), [driver](../scripts/ci/test-populated-migration.py) | Deterministic regressions, fixed-seed model/mutant tests and public artifact retention are integrated. Local final verification passed; the latest fresh remote run failed before population; startup/SDK corrections are locally verified; another fresh CI run is pending; the evidence ledger records actual outcomes and failed attempts without relabeling them as success. |
| Obtain independent adversarial review | Review findings recorded in the evidence ledger and addressed in corresponding code/tests | Reviews found and resolved authorization/accounting, lineage, proof-height, partial-migration evidence and transport failure-path defects. The latest journal review was tool-blocked and remains a review gap. These reviews and local rehearsals are not an independent audit. |

The central populated local V1→V2→V3 criterion passed, including original assets/claims, new traffic and canonical revalidation. See the [recorded result](evidence/migration-462-local-result.json). Fresh CI acceptance and the explicitly listed review/rollback evidence gaps remain separate. Arbitrary approved successor logic can still steal later; conservation at handover does not remove governance trust. Immutable-policy/kernel faults, lost authority, unavailable history and irrecoverably expired clients remain explicit recovery limits.


## Review follow-up (#462)

| Requirement | Implementation | Executable evidence / limit |
|---|---|---|
| Durable reviewed intent across UTxO turnover | `migration.ts:authorizeMigration`, registry nonce/generation, `migration-transactions.ts` preflight, unchanged on-chain Begin checks | `migration.test.ts` real heartbeat at three boundaries; `migration-operator.test.ts` seeded root continuation; incompatible provider-response cases |
| Authenticated resumable inventory acceleration | `migration-inventory.ts`, `DeploymentIbcTree.remove`, operator loop | Independent-root cache retry/stale/competition/rollback tests; full seeded production operator at N=2/8/32/128; real chain rollback unverified |
| Meaningful property populations/histories | `migration-history.test.ts`, `migration-operator.test.ts`, multi-channel accounting fixture | 100 independent economic histories; 16 actual compiled migration populations; no claim of generated two-chain packet settlement |
| Explicit deployment capability | `deployment-mode.ts`, deployment API, manifest/runtime normalization and canonical registry checks | Invalid/missing configuration rejection; Gateway manifest tests, SDK missing-capability test; intentional legacy must be explicit |
| Candidate provenance and limits | [Review report](migration-review-followup.md), `evidence/migration-review-result.json` | Exact source/artifact hashes and executed commands; previous populated acceptance separately identified |

Immediate emergency restriction is still missing. Compatible replacement cannot repair immutable policy/kernel/proof dependencies or implement arbitrary state transformation.
